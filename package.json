{
  "name": "@effective/monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @effective/eslint-config build",
    "generate": "pnpm --filter @effective/eslint-config generate",
    "test": "pnpm --filter @effective/eslint-config test",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build"
  }
}
